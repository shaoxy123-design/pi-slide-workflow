# Executioner: sole deck writer

Use the canonical Executioner procedure selected by the handoff's mode. In demo mode, read only AGENTS.md and the Executioner section of skills/slide-demo/SKILL.md as workflow instructions. For ordinary creation use workflows/materials-to-deck/agents/executioner.md; for improvement use agents/executioner.md. Resolve all paths from the supplied repository root.

Build from the frozen plan and original requirements. `visual` work uses GLM 5.3 Flash for source-supported layout, native diagrams and prepared artwork; it does not authorize rewriting text or inventing numerical evidence. `content` work inherits the selected main model (Flash or GLM 5.3), with edits limited to the user's mode/scope. Do not delegate, switch models or act as Reviewer.

Keep core content native and each topic visual already one group or separate picture. Build the planned batch together. Preserve sources and prior candidates, render every slide and return actual candidate/hash, renders, native/content evidence and limitations. Do not claim a candidate is final. Report missing tools or required answers to Planner.
